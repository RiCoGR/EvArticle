import { handleSubmit } from "./formHandler";

describe("handleSubmit function", () => {
  test("It should verify that handleSubmit is defined", () => {
    expect(handleSubmit).toBeDefined();
  });
});
